const initialstate = {}

export const forgotReducer = (state: any = initialstate, action: any) => {
    switch (action.type) {
        
        default: return state
    }
}

// actions

// thunks

// types