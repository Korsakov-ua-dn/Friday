const initialstate = {}

export const setNewPasswordReducer = (state: any = initialstate, action: any) => {
    switch (action.type) {
        
        default: return state
    }
}

// actions

// thunks

// types