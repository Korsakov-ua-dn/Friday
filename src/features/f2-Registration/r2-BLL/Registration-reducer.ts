const initialstate = {}

export const registrationReducer = (state: any = initialstate, action: any) => {
    switch (action.type) {
        
        default: return state
    }
}

// actions

// thunks

// types