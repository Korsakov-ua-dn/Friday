const initialstate = {}

export const profileReducer = (state: any = initialstate, action: any) => {
    switch (action.type) {
        
        default: return state
    }
}

// actions

// thunks

// types