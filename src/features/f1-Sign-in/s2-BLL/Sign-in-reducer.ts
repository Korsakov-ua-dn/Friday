const initialstate = {}

export const signInReducer = (state: any = initialstate, action: any) => {
    switch (action.type) {
        
        default: return state
    }
}

// actions

// thunks

// types