
const initialState = {}

export const signInReducer = (state: any = initialState, action: any) => {
    switch (action.type) {
        default:
            return state
    }
}

// actions

// thunks

// types
